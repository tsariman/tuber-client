import { type IRedux } from 'src/state';

export default function create_new_listing(redux: IRedux) {
  void redux;
  return async () => {
    // 1. get appbarsearchquery state
    // 2. Use it to get the value of the search query
  };
}