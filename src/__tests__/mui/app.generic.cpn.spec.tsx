import { describe, it, expect } from 'vitest'
import * as F from '../../mui/app.generic.cpn'

describe('src/mui/app.generic.cpn', () => {
  it('should import without error', () => {
    expect(F).toBeTruthy()
  })
})
