import { describe, it, expect } from 'vitest'
import * as F from '../../mui/app.complex.cpn'

describe('src/mui/complex.app.cpn', () => {
  it('should import without error', () => {
    expect(F).toBeTruthy()
  })
})