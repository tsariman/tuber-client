import { describe, it, expect } from 'vitest';
import * as F from '../../mui/complex.app.cpn';

describe('src/components/complex.app.component.tsx', () => {
  it('should import without error', () => {
    expect(F).toBeTruthy();
  });
});